# 设计令牌

`tokens.wxss` 只定义语义化视觉变量，不包含具体页面布局。

- `primary/meal`：主要操作和饮食场景。
- `life`：生活清单与生活记录。
- `success/warning/danger`：完成、提醒和危险操作。
- 字号、间距、圆角和阴影只从既有等级中选择；新增等级前先确认现有令牌无法表达。
- 核心点击区域使用 `--tap-min-size`，底部固定区域另外叠加 `env(safe-area-inset-bottom)`。

目前首页和第一批公共组件作为试点。其他页面应在各自独立任务中逐步接入，避免一次性视觉回归。
