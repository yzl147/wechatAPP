<view class="container">
  <!-- 搜索栏 -->
  <view class="search-bar">
    <input class="search-input" placeholder="搜索美食..." value="{{searchText}}" bindinput="onSearchInput" confirm-type="search" />
  </view>

  <!-- 分类标签 -->
  <scroll-view class="category-scroll" scroll-x enhanced show-scrollbar="{{false}}">
    <view class="category-list">
      <text 
        class="category-item {{currentCategory === item ? 'active' : ''}}" 
        wx:for="{{categories}}" 
        wx:key="*this"
        data-category="{{item}}"
        bindtap="onCategoryTap"
      >{{item}}</text>
    </view>
  </scroll-view>

  <!-- 美食列表 -->
  <view class="food-list">
    <view class="food-card" wx:for="{{filteredList}}" wx:key="id" bindtap="onFoodTap" data-id="{{item.id}}">
      <view class="food-image-wrap" style="background: {{item.bgStyle}};">
        <text class="food-icon">{{item.icon}}</text>
      </view>
      <view class="food-info">
        <view class="food-header">
          <text class="food-name">{{item.name}}</text>
          <view class="food-tag tag-{{item.catClass}}">{{item.category}}</view>
        </view>
        <text class="food-brief">{{item.brief}}</text>
        <view class="food-meta">
          <text class="meta-item">⏱ {{item.time}}</text>
          <text class="meta-item">📊 {{item.difficulty}}</text>
        </view>
      </view>
      <view class="arrow">›</view>
    </view>
  </view>

  <!-- 空状态 -->
  <view class="empty" wx:if="{{filteredList.length === 0}}">
    <text class="empty-text">没有找到相关美食</text>
  </view>
</view>
